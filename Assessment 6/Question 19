#include <stdio.h>
int main()
{
    int num,mid,flag=0,i=2;
    scanf("%d",&num);
    mid=(num/10)%100;
    if(mid<=1)
    {
        printf("Not Prime");
        return 0;
    }
    while(i<=mid/2)
    {
        if(mid%i==0)
        {
            flag=1;
            break;
        }
        i++;}
    if(flag==0)
    printf("Prime");
    else printf("Not Prime");
    return 0;
}
