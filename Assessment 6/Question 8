#include <stdio.h>
int main()
{
    int i = 10, tens, units, sum;
    while(i <= 99)
    {
        tens = i / 10;
        units = i % 10;
        sum = tens + units;
        if(i % 2 == 0 && sum == 6)
            printf("%d\n", i);
        i++;
    }
    return 0;
}
