#include <stdio.h>
int main()
{
    int i, sum, tens, units;
    for(i=10;i<=99;i++)
    {
        tens = i / 10;
        units = i % 10;
        sum = tens + units;
        if(i%2 != 0 && sum == 7)
            printf("%d \n", i);
    }
    return 0;
}
