#include <stdio.h>
int main()
{
    int num,i=2,result=0;
    scanf("%d",&num);
    if(num<=1){printf("Not Prime");return 0;}
    while(i<=num/2)
    {
        if(num%i==0){result=1;break;}
        i++;
    }
    if(result==0)printf("Prime");
    else printf("Not Prime");
    return 0;
}
