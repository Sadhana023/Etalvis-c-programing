#include<stdio.h>
int main()
{
    int x,unit,tens,sum;
    printf("Enter x: ");
    scanf("%d",&x);
    unit=x%10;
    tens=(x/10)%10;
    sum=(x/100)+tens+unit;
    printf("%d",sum);
    return 0;
}
