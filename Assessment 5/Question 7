#include<stdio.h>
int main()
{
    int i,tens,unit,sum=0;
    for(i=10;i<=99;i++)
    {
        unit=i%10;
        tens=i/10;
        sum=unit+tens;
        if(i%2 != 0 && sum==7)
        printf("%d \n",i);
       
    }
    return 0;
}
