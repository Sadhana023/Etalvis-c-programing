#include <stdio.h>
int main()
{
    int num, last, rem, result;
    scanf("%d", &num);
    last = num % 10;          
    rem = num / 10;           
    if (last % 2 == 0)
        result = num;         
    else
        result = rem * 10 + (last - 1);  
    printf("%d", result);
    return 0;
}
