#include <stdio.h>
int check_prime(int num)
{
    if(num<2) return 0;
    int i=2;
    while(i*i<=num)
    {
        if(num%i==0) return 0;
        i++;
    }
    return 1;
}
int main()
{
    int num;
    scanf("%d",&num);
    if(check_prime(num)) 
    printf("prime");
    else printf("not prime");
    return 0;
}
