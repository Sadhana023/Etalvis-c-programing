#include <stdio.h>
int check_first_2digits_prime(int num)
{
    if(num<0) num=-num;
    int temp=num,digits=0,p=1;
    while(temp>0)
    {
        temp/=10;
        digits++;
    }
    if(digits<2) 
    return 0;  // less than 2 digits
    temp=num;
    for(int i=1;i<digits-2;i++) p*=10;
    int first2=num/p;
    if(first2<2) return 0;
    int i=2;
    while(i*i<=first2)
    {
        if(first2%i==0) 
        return 0;
        i++;
    }
    return 1;
}
int main()
{
    int num;
    scanf("%d",&num);
    if(check_first_2digits_prime(num)) 
    printf("prime");
    else printf("not prime");
    return 0;
}
