#include <stdio.h>
int disp_reverse_number(int num)
{
    int rev=0;
    if(num<0) num=-num;
    while(num>0)
    {
        rev = rev*10 + num%10;
        num /= 10;
    }
    return rev;
}
int main()
{
    int num;
    scanf("%d",&num);
    printf("%d",disp_reverse_number(num));
    return 0;
}
